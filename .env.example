# Read-only local path synced by Google Drive for desktop.
PENSION_DATA_ROOT=/absolute/path/to/google-drive/연금

# Generated local artifacts. Do not point these to the company source folder.
PARSED_DATA_ROOT=./data/parsed
INDEX_DATA_ROOT=./data/indexes

# HyperCLOVA X credentials (set real values only in the untracked .env file)
HCX_API_KEY=
HCX_API_URL=
